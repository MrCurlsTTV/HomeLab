apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: grafana-vs
spec:
  hosts:
  - "grafana.apps.homelab.com"
  gateways:
  - main-gateway
  http:
  - route:
    - destination:
        host: grafana
        port:
          number: 3000 